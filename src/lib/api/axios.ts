// This file is kept for backward compatibility
// It now just passes through to our NextAuth client

import nextAuthClient from './nextAuthClient';

const apiClient = nextAuthClient;

export default apiClient; 